#拖拽
javascript实现的拖拽小程序


##拖拽有哪些功能？

* `Drag.js`支持随意拖拽
* `LimitDrag.js`支持在浏览器屏幕内进行拖拽
* `LimitAreaDrag.js`只支持在父节点元素区域内进行拖拽
* 使用面向对象编程，实现了属性和方法的继承及扩展


##使用方法

```javascript
 new Drag(id);
 new LimitDrag(id);
 new LimitAreaDrag(id);
```